version: '3.8'

services:
  sonarqube:
    image: sonarqube:community
    container_name: local_sonarqube
    ports:
      - "9000:9000"
    networks:
      - sonar_net

networks:
  sonar_net:
    driver: bridge
